# Amnezia runtime directory

This directory is mounted into the AmneziaWG runtime container.

Keep runtime-local files and generated interface artifacts here.
Do not manage peers manually from desktop apps; node-agent is the only runtime controller.
