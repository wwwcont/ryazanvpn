services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    env_file:
      - .env.single.generated
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:8-alpine
    restart: unless-stopped
    command: ["redis-server", "--save", "", "--appendonly", "no", "--requirepass", "${REDIS_PASSWORD}"]
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  migrate:
    image: migrate/migrate:v4.17.1
    restart: "no"
    command: ["-path", "/migrations", "-database", "${POSTGRES_URL}", "up"]
    volumes:
      - ./migrations:/migrations:ro
    env_file: [.env.single.generated]
    depends_on:
      postgres:
        condition: service_healthy

  control-plane:
    build:
      context: .
      args:
        SERVICE: cmd/control-plane
    restart: unless-stopped
    env_file: [.env.single.generated]
    environment:
      HTTP_ADDR: ${CONTROL_PLANE_HTTP_ADDR:-:8080}
      CONTROL_PLANE_HTTP_ADDR: ${CONTROL_PLANE_HTTP_ADDR:-:8080}
      CONFIG_MASTER_KEY: ${CONFIG_MASTER_KEY:?CONFIG_MASTER_KEY is required}
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  amnezia-awg:
    build:
      context: .
      dockerfile: deploy/amneziawg-go/Dockerfile
    env_file: [.env.single.generated]
    container_name: ${AMNEZIA_CONTAINER_NAME:?AMNEZIA_CONTAINER_NAME is required}
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv6.conf.all.forwarding=1
    environment:
      - AMNEZIA_INTERFACE_NAME=${AMNEZIA_INTERFACE_NAME:?AMNEZIA_INTERFACE_NAME is required}
      - AMNEZIA_CONFIG_DIR=/etc/amnezia
      - AMNEZIA_KEEP_INTERFACE_UP=1
    volumes:
      - single_amnezia_data:/var/lib/amnezia
      - ./deploy/node/amnezia:/etc/amnezia:rw
    healthcheck:
      test: ["CMD-SHELL", "awg show ${AMNEZIA_INTERFACE_NAME:?AMNEZIA_INTERFACE_NAME is required} >/dev/null 2>&1"]
      interval: 20s
      timeout: 5s
      retries: 5

  xray:
    image: teddysun/xray:1.8.24
    env_file: [.env.single.generated]
    container_name: ${XRAY_CONTAINER_NAME:-ryazanvpn-xray}
    restart: unless-stopped
    command: ["xray", "run", "-config", "/etc/xray/config.json"]
    volumes:
      - ./deploy/node/xray/config.json:/etc/xray/config.json:ro
      - single_xray_data:/var/lib/xray
      - single_xray_logs:/var/log/xray
    healthcheck:
      test: ["CMD", "xray", "version"]
      interval: 20s
      timeout: 5s
      retries: 5

  node-agent:
    build:
      context: .
      args:
        SERVICE: cmd/node-agent
    restart: unless-stopped
    env_file: [.env.single.generated]
    environment:
      HTTP_ADDR: ${NODE_AGENT_HTTP_ADDR:-:8081}
      NODE_AGENT_HTTP_ADDR: ${NODE_AGENT_HTTP_ADDR:-:8081}
      DOCKER_BINARY_PATH: ${DOCKER_BINARY_PATH:-docker}
      RUNTIME_ADAPTER: ${RUNTIME_ADAPTER:-amnezia_docker}
      AMNEZIA_CONTAINER_NAME: ${AMNEZIA_CONTAINER_NAME:?AMNEZIA_CONTAINER_NAME is required}
      AMNEZIA_INTERFACE_NAME: ${AMNEZIA_INTERFACE_NAME:?AMNEZIA_INTERFACE_NAME is required}
      XRAY_CONTAINER_NAME: ${XRAY_CONTAINER_NAME:-ryazanvpn-xray}
    ports:
      - "8081:8081"
    depends_on:
      amnezia-awg:
        condition: service_started
      xray:
        condition: service_started
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  postgres_data:
  single_amnezia_data:
  single_xray_data:
  single_xray_logs:
